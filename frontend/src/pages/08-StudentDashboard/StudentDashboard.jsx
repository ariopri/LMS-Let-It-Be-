import React from 'react';
import Hero from '../../components/03-C.StudentDashboard/hero/hero';

const StudentDashboard = () => {
    return (
        <div>
            <Hero/>
        </div>
    );
};

export default StudentDashboard;